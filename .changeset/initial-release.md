---
"@dennation/menu": minor
---

Initial release — a headless, typesafe, router-agnostic navigation menu: the `defineMenu` data model, a render-prop `<Menu>` renderer, the `useMenu` hook, and a TanStack Router adapter.
